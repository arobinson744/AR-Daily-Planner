# AR Daily Planner

Private source repository for the AR Daily Planner.

The app uses Supabase Authentication and a Row Level Security-protected `planner_state` table for cloud sync across devices. `config.js` contains only the browser-safe Supabase publishable key; never add a service-role or secret key to this repository.
